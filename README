Progress bar.

While a process starts on background function must be called 
which monitors the latest created process untill it finished.

How to use
Call the function with arg ie 1, speed to display. 
Arg 1 means 1 sec delay.

Enjoy! 
